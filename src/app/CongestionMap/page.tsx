import DynamicCongestionMap from '@/components/DynamicCongestionMap'; // 👈 Import the wrapper

export default function CongestionMapPage() {
    return (
        <div>
            {/* Render the Client Component wrapper */}
            <DynamicCongestionMap />
        </div>
    );
}